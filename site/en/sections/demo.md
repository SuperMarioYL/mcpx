![mcpx demo - one mcpx add installs into every client and handshake-verifies each](/assets/demo.gif)

*One `mcpx add`: detect the clients on this machine → back up → write each client's own schema → handshake-verify each one — ending in `3/3 clients ready`.*
